# Grafana 数据源自动配置（T025）：启动即注册 Prometheus，无需 UI 手工添加
apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090  # compose 服务名
    isDefault: true
    editable: false
